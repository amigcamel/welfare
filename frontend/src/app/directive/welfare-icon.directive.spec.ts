import { WelfareIconDirective } from './welfare-icon.directive';

describe('WelfareIconDirective', () => {
  it('should create an instance', () => {
    const directive = new WelfareIconDirective(null);
    expect(directive).toBeTruthy();
  });
});
