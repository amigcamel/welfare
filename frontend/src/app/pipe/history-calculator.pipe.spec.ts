import { HistoryCalculatorPipe } from './history-calculator.pipe';

describe('HistoryCalculatorPipe', () => {
  it('create an instance', () => {
    const pipe = new HistoryCalculatorPipe();
    expect(pipe).toBeTruthy();
  });
});
