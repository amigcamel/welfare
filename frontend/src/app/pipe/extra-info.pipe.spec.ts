import { ExtraInfoPipe } from './extra-info.pipe';

describe('ExtraInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtraInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
